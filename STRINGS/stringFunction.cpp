// VARIOUS STRING FUNTION.
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
using namespace std;
int main()
{
    string a;
   cout << "Enter the string:-";
   getline(cin, a);
    // REVERSE A STRING FUNCTION.
    //  1. reverse(a.begin(),a.end());
    // cout<<"Reverse String is:-"<<a;*/

    // 2. SUBSTR THAT CAN BE USED TO FIND A PART OF STRING FROM A GIVEN STRING.
    // cout<<"Substring is:-"<<a.substr(0,2);

    // 3. + operator that can be used to add string.
   // string b;
   // cout << "Enter the string:-";
   // getline(cin, b);
  //  cout << "Concatenated string is:-" << a << " " + b;

  // 4. strcat function that can be used to add two character.
 // char a[10]="Rohan";
  //char b[10]="Raj";
 // strcat(a ,b);
 // cout<<a;

 // 5. push back that can be used to add string at the end.
//char c='l';
//a.push_back(c);
//cout<<a;

// 6. find size or length of a string/character.
//cout<<"Size of a given string is:-"<<a.size()<<endl;
//cout<<"Length of a given character is:-"<<strlen(b);

// 7. Convert any integer number to string.
//int ab=8;
//cout<<to_string(ab);
    return 0;
}